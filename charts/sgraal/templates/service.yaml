apiVersion: v1
kind: Service
metadata:
  name: {{ include "sgraal.fullname" . }}
  labels:
    {{- include "sgraal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sgraal.selectorLabels" . | nindent 4 }}
