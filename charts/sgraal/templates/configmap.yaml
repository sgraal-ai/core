apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sgraal.fullname" . }}-config
  labels:
    {{- include "sgraal.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
